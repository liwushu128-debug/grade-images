interface:
  display_name: "Grade Images"
  short_description: "Strict grading with opt-in output sharpening"
  default_prompt: "Use $grade-images to grade my photo and render a preview. Keep texture unchanged unless I explicitly request bounded output sharpening; never repair, generate, or alter scene content."
